# Optional — for managed-agent cookbook lint and schema validation only.
# Most PR checks use the stdlib; install once: python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
jsonschema>=4.0
PyYAML>=6.0
