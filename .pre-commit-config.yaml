# Local hooks for claude-for-strategy. Install: pre-commit install
repos:
  - repo: local
    hooks:
      - id: check-skill-frontmatter
        name: check skill frontmatter
        entry: python3 scripts/check-skill-frontmatter.py --check
        language: system
        files: SKILL\.md$
        pass_filenames: true
