# Optional local hook — install with: pre-commit install
# Runs step 6 from CLAUDE.md on staged SKILL.md files only.
repos:
  - repo: local
    hooks:
      - id: validate-skills
        name: validate skills
        entry: python3 scripts/validate-skills.py --check
        language: system
        files: (^|/)skills/.+/SKILL\.md$
